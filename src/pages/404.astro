---
import Layout from '../layouts/Layout.astro';
import '../styles/global.css';
---
<Layout title="404 — Halaman Tidak Ditemukan | HurufLED">
  <style>
    .not-found {
      min-height: 100vh;
      background: var(--bg);
      display: flex;
      flex-direction: column;
      align-items: center;
      justify-content: center;
      text-align: center;
      padding: 2rem;
      gap: 1.5rem;
      position: relative;
      overflow: hidden;
    }
    .not-found::before {
      content: '';
      position: absolute; inset: 0;
      background-image:
        linear-gradient(rgba(0,245,255,.03) 1px, transparent 1px),
        linear-gradient(90deg, rgba(0,245,255,.03) 1px, transparent 1px);
      background-size: 60px 60px;
    }
    .err-num {
      font-family: 'Orbitron', sans-serif;
      font-size: clamp(5rem, 15vw, 10rem);
      font-weight: 900;
      color: var(--neon-cyan);
      text-shadow: 0 0 20px var(--neon-cyan), 0 0 60px rgba(0,245,255,.4);
      line-height: 1;
      position: relative;
    }
    .err-title {
      font-family: 'Orbitron', sans-serif;
      font-size: 1.5rem;
      font-weight: 700;
      color: var(--text-bright);
      position: relative;
    }
    .err-text {
      color: var(--text-muted);
      max-width: 400px;
      position: relative;
    }
  </style>
  <div class="not-found">
    <div style="font-size:4rem;position:relative;">🪧</div>
    <div class="err-num">404</div>
    <h1 class="err-title">Halaman Tidak Ditemukan</h1>
    <p class="err-text">Maaf, halaman yang Anda cari tidak ada. Mungkin sudah dipindahkan atau dihapus.</p>
    <a href="/" class="btn btn-primary" style="position:relative;">← KEMBALI KE BERANDA</a>
  </div>
</Layout>
